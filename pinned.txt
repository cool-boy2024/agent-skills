# Pinned skills — never removed by `clean.sh` without explicit `--force`.
# Format: one skill name per line. Lines starting with `#` are comments.
# `install.sh` auto-appends a new line when a skill is installed.
# Use `./scripts/unpin.sh <name>` to remove a name from this file (then `./scripts/clean.sh <name>` will actually delete it).
